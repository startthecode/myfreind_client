import { Icon } from "@iconify/react";

export const Save = () => {
  const saveIconFinalAttribute = {
    height: 30,
    icon: "formkit:bookmark",
  };
  return <Icon {...saveIconFinalAttribute} />;
};
